{
  "name": "ai-works",
  "version": "0.3.0",
  "private": true,
  "description": "AI WORKS public-interest landing page and constrained public-service router.",
  "license": "Unlicense",
  "scripts": {
    "dev": "node server.js",
    "test": "node scripts/test-router.js && node scripts/test-provider.js && python3 portal/lint.py data/records.us.json"
  },
  "engines": {
    "node": ">=20"
  }
}
